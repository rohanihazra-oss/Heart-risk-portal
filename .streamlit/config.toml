[theme]
# This prevents the "invisible text" error by forcing Dark Mode
base = "dark"
primaryColor = "#4FD1C5"
backgroundColor = "#1A202C"
secondaryBackgroundColor = "#2D3748"
textColor = "#E2E8F0"

[server]
# Stops the app from crashing during system theme changes
runOnSave = true
headless = true
